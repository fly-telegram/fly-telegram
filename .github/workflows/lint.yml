name: Lint

on:
  push:
    branches: [master, v2]
  pull_request:
    branches: [master, v2]

jobs:
  ruff:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          version: "0.15.14"
